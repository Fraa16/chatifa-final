{
  "name": "chatifa",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp *.html fonts.css robots.txt sitemap.xml llms.txt dist/ && cp -r assets fonts dist/",
    "dev": "serve . --listen 5173",
    "start": "serve . --listen 5173"
  },
  "dependencies": {
    "serve": "^14.2.3"
  }
}
